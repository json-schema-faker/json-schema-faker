name: build

on:
  push:
    branches:
    - master
    - develop
  pull_request:
    branches:
    - master

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          make ci CODECOV_TOKEN=${{ secrets.CODECOV_TOKEN }}
