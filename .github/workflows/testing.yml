name: build

on:
  push:
    branches:
    - develop
  pull_request:
    branches:
    - master
    - develop

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: tacoss/nodejs@master
        with:
          args: make ci
