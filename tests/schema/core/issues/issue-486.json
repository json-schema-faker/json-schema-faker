[
  {
    "description": "pattern constraints",
    "tests": [
      {
        "description": "should fullfil minLength/maxLength constraints",
        "schema": {
          "type": "string",
          "minLength": 8,
          "maxLength": 20,
          "pattern": "^[a-zA-Z0-9_]+$"
        },
        "valid": true
      }
    ]
  }
]
