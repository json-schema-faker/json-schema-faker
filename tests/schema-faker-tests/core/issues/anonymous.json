[
  {
    "description": "anonymous schemas",
    "tests": [
      {
        "description": "should not work as expected",
        "skip": true,
        "schema": {
          "x": {
            "enum": ["y"]
          }
        },
        "valid": true,
        "equal": {
          "x": "y"
        }
      }
    ]
  }
]
