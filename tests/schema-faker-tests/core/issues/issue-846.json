[
  {
    "description": "Issue #846 - Meta data (description) leaked as property from merged $ref",
    "tests": [
      {
        "description": "simple case: $ref with sibling description should not add description as a property",
        "schema": {
          "type": "object",
          "properties": {
            "name": {
              "$ref": "#/$defs/Name",
              "description": "The user's name"
            }
          },
          "required": ["name"],
          "additionalProperties": false,
          "$defs": {
            "Name": {
              "type": "object",
              "properties": {
                "first": { "type": "string" },
                "last": { "type": "string" }
              },
              "additionalProperties": false
            }
          }
        },
        "valid": true
      },
      {
        "description": "should not leak description as a property when $ref has sibling description",
        "schema": {
          "description": "test",
          "type": "object",
          "properties": {
            "nested": {
              "$ref": "#/$defs/Nested"
            }
          },
          "required": ["nested"],
          "additionalProperties": false,
          "$defs": {
            "Nested": {
              "description": "This won't be in the generated data!",
              "type": "object",
              "properties": {
                "no_bug_here": {
                  "$ref": "#/$defs/CorruptedWhenNested"
                },
                "buggedPropertyWhenNested": {
                  "type": "object",
                  "$ref": "#/$defs/Nested2"
                }
              },
              "additionalProperties": false
            },
            "CorruptedWhenNested": {
              "type": "object",
              "properties": {
                "buggedProperty": {
                  "description": "Where is name and address?",
                  "$ref": "#/$defs/NestedDefinitionWhichGetsCorrupted"
                },
                "buggedProperties": {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/NestedDefinitionWhichGetsCorrupted"
                  }
                }
              },
              "additionalProperties": false
            },
            "NestedDefinitionWhichGetsCorrupted": {
              "type": "object",
              "properties": {
                "name": { "type": "string" },
                "address": { "type": "string" }
              },
              "additionalProperties": false
            },
            "Nested2": {
              "type": "object",
              "properties": {
                "will_bug_here": {
                  "$ref": "#/$defs/CorruptedWhenNested"
                }
              },
              "additionalProperties": false
            }
          }
        },
        "valid": true
      }
    ]
  }
]
