[
  {
    "description": "Issue #778 - Invalid data for strings with both pattern and format (known limitation)",
    "tests": [
      {
        "description": "string with only format=date-time generates a valid date-time",
        "schema": {
          "type": "string",
          "format": "date-time"
        },
        "valid": true
      },
      {
        "description": "string with only pattern generates a pattern-matching string",
        "schema": {
          "type": "string",
          "pattern": "^202[0-9]-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
        },
        "valid": true
      },
      {
        "description": "known limitation: pattern takes precedence over format, result matches pattern but may not be a valid date-time",
        "schema": {
          "type": "string",
          "pattern": "^202.*Z$",
          "format": "date-time"
        },
        "valid": false
      }
    ]
  }
]
