[
  {
    "description": "root-definitions on sync mode",
    "tests": [
      {
        "description": "should resolve local definitions",
        "schema": {
          "$defs": {
            "foo": {
              "type": "object",
              "properties": {
                "bar": {
                  "type": "string"
                }
              }
            }
          },
          "$ref": "#/$defs/foo"
        },
        "sync": true,
        "valid": true
      }
    ]
  }
]
